import 'package:flutter/material.dart';

class DisciplineForm  extends StatelessWidget {
  const DisciplineForm ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Cadastrar Disciplina")
      ),
    );
  }
}